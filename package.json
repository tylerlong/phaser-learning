{
  "license": "MIT",
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  },
  "dependencies": {
    "phaser": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.5",
    "gts": "^2.0.2",
    "typescript": "^3.9.3",
    "yarn-upgrade-all": "^0.5.2"
  }
}
